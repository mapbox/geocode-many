{
  "name": "geocode-many",
  "version": "1.0.1",
  "description": "Given an array of objects and a transform into a geocodable string, geocode them if possible.",
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production browserify index.js -s geocodemany -o geocodemany.js",
    "start": "watchify index.js -s geocodemany -o geocodemany.js & serve"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/mapbox/geocode-many.git"
  },
  "keywords": [
    "geocode",
    "geo"
  ],
  "author": "@tmcw",
  "license": "BSD",
  "bugs": {
    "url": "https://github.com/mapbox/geocode-many/issues"
  },
  "homepage": "https://github.com/mapbox/geocode-many",
  "dependencies": {
    "queue-async": "1.0.7",
    "xhr": "^2.0.1"
  },
  "devDependencies": {
    "browserify": "^9.0.8"
  }
}
